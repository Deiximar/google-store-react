import NavLinks from "./NavLinks";
import IconsNav from "./IconsNav";

const NavBar = () => {
  return (
    <header>
      <NavLinks />
      <IconsNav />
    </header>
  );
};

export default NavBar;
